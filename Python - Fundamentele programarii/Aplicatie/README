I wrote an application that has a console-type user interface with a menu. Here we have implemented finding in a list the maximum length sequence that has different properties.
The application documentation contains run scenarios and test cases for requirements in tabular format.
